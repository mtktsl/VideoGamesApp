import XCTest
@testable import NSLayoutConstraintExtensionPackage

final class NSLayoutConstraintExtensionPackageTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        //XCTAssertEqual(NSLayoutConstraintExtensionPackage().text, "Hello, World!")
    }
}
