import XCTest
@testable import NetworkStatusObserver

final class NetworkStatusObserverTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        //XCTAssertEqual(InternetStatusController().text, "Hello, World!")
    }
}
